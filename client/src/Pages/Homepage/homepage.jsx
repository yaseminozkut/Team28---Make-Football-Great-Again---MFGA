import React from "react";
import { ContainerDiv, InnerContainer } from "./HomepageElements";

const Homepage = () => {
  return (
    <ContainerDiv>
      <InnerContainer>
        Homepage
      </InnerContainer>
    </ContainerDiv>
  );
};

export default Homepage;
